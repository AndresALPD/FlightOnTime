# Configuracion del servidor
server.port=8080
spring.application.name=flight-ontime-api

# Logging
logging.level.root=INFO
logging.level.com.flightontime=DEBUG

# Manejo de errores
spring.web.error.include-message=always
spring.web.error.include-binding-errors=always

# Conexion base de datos
spring.datasource.url=jdbc:mysql://database-retraso-vuelos.cb4i8y08sidt.sa-east-1.rds.amazonaws.com:3306/db_vuelos
spring.datasource.username=admin
spring.datasource.password=Vuelos1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate crea las tablas automaticamente si no existen
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
